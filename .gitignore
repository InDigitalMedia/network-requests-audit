.DS_Store

# Per-user BMAD install config — regenerated by the installer, scoped to one
# person's local answers. See _bmad/custom/.gitignore for the same pattern
# applied to _bmad/custom/.
_bmad/config.user.toml
